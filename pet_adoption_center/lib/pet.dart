// Q1: Pet class to store pet information
// This class has 3 attributes: name, breed, and age (all Strings)

class Pet {
  String name;
  String breed;
  String age;

  // Constructor that takes in 3 strings to initialize the variables
  Pet(this.name, this.breed, this.age);
}
